#include <stdio.h>
#include <stdlib.h>
void issue7()
{
    char q,w,e,r,t,y,u,i,o,p,a,s,d,f,g,h,j,k,l,z,x,c,v,b,n,m,ch;

    scanf("%c",&ch);
    if(ch=='a'||ch=='e'||ch=='i'||ch=='o'||ch=='u')

       printf("%c is a vowel",ch);

    else
    {
        printf("%c is a consonant",ch);
    }
}

